'use client'

// Reserved for future client-side providers (e.g. auth context, toast, analytics).
// Currently unused — add providers here as needed and import in app/layout.tsx.
export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
